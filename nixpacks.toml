[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "dejavu_fonts", "noto-fonts", "noto-fonts-cjk", "zlib", "libjpeg", "gcc", "libffi", "openssl"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python main.py"
